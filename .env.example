# Django Marketing Tracker - Environment Variables Example
# Copy this file to .env and fill in your values for local development
# DO NOT commit .env to version control - it contains secrets!

# ============================================================================
# ENVIRONMENT CONFIGURATION
# ============================================================================
# Set to 'DEV' for local development or 'AWS' for AWS deployment (EC2, ECS, Lambda, EB)
ENVIRONMENT=DEV

# ============================================================================
# DJANGO SETTINGS
# ============================================================================
DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# DEV: Uses SQLite by default (no DATABASE_URL needed)
# AWS: Requires DATABASE_URL for PostgreSQL on AWS RDS
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ============================================================================
# AWS S3 CONFIGURATION (Only needed when ENVIRONMENT=AWS)
# ============================================================================
# Required for AWS environment to store static and media files on S3
# AWS_STORAGE_BUCKET_NAME=your-s3-bucket-name
# AWS_S3_REGION_NAME=us-east-1

# Optional: AWS credentials (not needed if using IAM roles on EC2)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key

# ============================================================================
# ENVIRONMENT NOTES
# ============================================================================
# DEV Environment:
#   - Uses SQLite database
#   - Stores static/media files locally
#   - DEBUG=True recommended
#
# AWS Environment:
#   - Requires DATABASE_URL for RDS PostgreSQL
#   - Stores static/media files on S3
#   - Requires AWS_STORAGE_BUCKET_NAME
#   - DEBUG=False recommended
#   - AWS credentials optional if using IAM roles
#
# Elastic Beanstalk:
#   - Set ENVIRONMENT=AWS
#   - Configure environment variables in EB Console
#   - EB configuration files in .ebextensions/ are preserved

